# 3D打印异常检测系统依赖
# 安装命令: pip install -r requirements.txt
#
# 【硬件适配说明】
# 本文件默认配置为 NVIDIA GPU (CUDA 12.1) 环境
# 移植到其他硬件时，请根据下方注释修改对应包

# ==================== 深度学习框架 ====================
# 【GPU环境 - NVIDIA CUDA】默认配置，有NVIDIA显卡时使用
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# 【CPU环境】无显卡或仅CPU时，注释掉上面两行，取消注释下面两行
# torch==2.5.1+cpu
# torchvision==0.20.1+cpu

# YOLOv8目标检测框架（自动安装torch等依赖）
ultralytics==8.4.14

# ==================== 图像处理 ====================
# opencv-python-headless 是无GUI版本，适用于服务器/容器环境
# 如需无GUI版本，注释掉下行，取消注释headless版本
opencv-python==4.13.0.92
# opencv-python-headless==4.13.0.92

pillow==12.1.1

# ==================== Web服务 ====================
flask==3.1.3
flask-socketio==5.6.1
python-socketio==5.16.1
python-engineio==4.13.1

# ==================== 打印机控制 ====================
bambulabs-api==2.6.6
paho-mqtt==2.1.0

# ==================== 配置与数据处理 ====================
PyYAML==6.0.3
numpy==2.2.6

# ==================== 网络请求 ====================
requests==2.32.5
